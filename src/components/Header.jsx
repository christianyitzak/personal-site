import { NavLink } from "react-router";

function Header() {
    return (
        <header className="p-5 flex justify-between max-w-2xl mx-auto">
            <div>
                <p className="text-[#ffffff4d] text-sm font-semibold">
                    Christian Yitzak
                </p>
            </div>
        </header>
    );
}

export default Header;
